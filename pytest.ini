[pytest]
testpaths = tests backend/tests
norecursedirs = node_modules frontend/node_modules .venv env my_portfolio.com dist frontend/dist
pythonpath = .
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
